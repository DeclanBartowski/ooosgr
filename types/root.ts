/*
export interface Page {
    title: string
}

export interface Promo {
    title: string
    text: string
    img: string
}

export interface PromosCollection {
    [key: number]: Promo;
}

export interface ArticleCategory {
    code: string
    name: string
}

export interface ArticleCategoryCollection {
    [key: number]: ArticleCategory;
}

export interface Article {
    name: string
    img: string | null
    video: string | null
    category: ArticleCategory
    text: string
    code:string
    type:string
}

export interface ArticleCollection {
    [key: number]: Article;
}
*/
